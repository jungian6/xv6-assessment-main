struct device_context {
    int current_x, current_y; // Current position
    int current_pen; // Current pen color
};
struct rect {
    int top;    // y coordinate of the top of the rectangle
    int left;   // x coordinate of the left of the rectangle
    int bottom; // y coordinate of the bottom of the rectangle
    int right;  // x coordinate of the right of the rectangle
};
